#include <stdio.h>
/**
* main - This is the entry point of the code
*
* Return - 0 Successful
*/
int main(void)
{
char lowercase = 'a';
while (lowercase = 'z')
{
if (lowercase != 'q' || lowercase != 'e')
putchar(lowercase);
lowercase++;
}
putchar('\n');
return (0);
}
